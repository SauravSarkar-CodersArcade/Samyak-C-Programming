#include<stdio.h>
int main(){
    int arr[5] = {1,2}; // 1,2,0,0,0 
    // 12 bytes wasted = Efficient = Save 
    // DMA = Allocating memory during runtime
    // Program Execution  
    // malloc(), calloc(), realloc(), free()
    return 0;
}